.sftp.json
.vscode/sftp.json
.env
.env.*
*.env
*.local.ps1
!.env.example
pinapp-automation.env
.htpasswd
*.pem
*.key
*.p12
node_modules/
_site/
dist/
.DS_Store
Thumbs.db
*.log
*.local
tools/*.local.*
.htaccess.local
# Jetons pinapp.fr (copie depuis ton doc / EXAMPLE)
pinapp-fr-deploy.local.ps1
pinapp-stack.local.ps1
# Tout-en-un jetons (copie depuis pinapp-tes-clefs.EXAMPLE.ps1)
pinapp-tes-clefs.ps1

pinapp-fr-env*.ps1

# Branchement n8n / wa.me local (copie depuis pinapp-automation.local.EXAMPLE.ps1)
pinapp-automation.local.ps1

# Backups locaux (scripts Pandora)
**/home-2026.backup.*.css
index.backup.*.html
index.tdah-backup.*.html

# Upload batch Vimeo (reprise — chemins machine en clés)
tools/.pinapp_upload_state.json

# Archives de sauvegarde locales (ne pas versionner)
backups/*.zip
backups/*.tar.gz
