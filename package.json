{
  "name": "pinapp-site",
  "private": true,
  "type": "module",
  "description": "Site vitrine Pinapp — HTML/CSS/JS ; outillage Vite (dev) + Prettier",
  "scripts": {
    "dev": "vite --host",
    "format": "prettier --write \"**/*.{html,css,js,mjs,json,md,yml}\"",
    "format:check": "prettier --check \"**/*.{html,css,js,mjs,json,md,yml}\""
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "vite": "^8.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
