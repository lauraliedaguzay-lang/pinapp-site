/**
 * Logique hero-reveal : classe `hero-reveal-motion-ready` posée sur <html>
 * dès le <head> (voir index.html) pour éviter tout flash / double animation.
 * Ce fichier reste un point d’ancrage pour futures évolutions (split mots, etc.).
 */
(function () {
  "use strict";
})();
